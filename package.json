{
  "name": "study-buddy-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.12.1",
  "description": "Monorepo for Study Buddy academic graph collaboration web app",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "overrides": {
    "postcss": "^8.5.10",
    "tar": "^7.5.11",
    "next": {
      "postcss": "^8.5.10"
    }
  }
}
